/** The live site address, used for Google, the sitemap, structured data and link previews. */
export const SITE_URL = (process.env.NEXT_PUBLIC_SITE_URL ?? "https://skandiora.in").replace(/\/$/, "");
